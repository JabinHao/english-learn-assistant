services:
  postgres:
    image: postgres:16
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: ailearn
      POSTGRES_USER: ailearn
      POSTGRES_PASSWORD: ailearn
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
